{
  "name": "key-tuner-web",
  "version": "0.1.0",
  "description": "Web frontend for key-tuner password generator",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "vite": "^6.2.0",
    "vite-plugin-wasm-pack": "^0.1.12",
    "vitest": "^3.0.7"
  }
}
